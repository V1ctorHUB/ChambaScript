var speed           : int = 150;
var ticksPerSecond  : int = 1000;
var seconds         : int = 10000;
var ticks           : int = 0;
var targetTicks     : int = 0;

targetTicks = seconds * ticksPerSecond;

setForward(speed);

while (ticks < targetTicks) {
    accelerate(speed);
    ticks = ticks + 1;
};

brake();
